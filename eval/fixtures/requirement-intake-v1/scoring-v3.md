# Requirement intake scoring v3 — 2026-09-25

The input cases, oracle, original manifest and v2 semantic-review rules remain unchanged. V3 fixes the measured RI-10 false negative: source resolution considers every candidate matching the canonical key, value and unit, then filters against sources active at that checkpoint. It cannot use another value, another unit, withdrawn evidence, or an oracle to create provenance. The confirmation guard applies the same full-candidate search and active-source boundary.

Re-score the saved v2 captures with their existing capture-bound semantic reviews before comparing with new model outputs. Keep the original report and code unchanged in the evidence archive. Re-scoring cannot fill unexecuted checkpoints or turn an earlier failed execution into a completed task. The corrected baseline is 0/12 tasks and 9/24 observed checkpoints, with 7 additional planned checkpoints unreached; the one-point correction is measurement repair, not a Harness improvement.

The new trial uses the same model alias, input/output budgets and production timeouts as v2. It exercises repaired production behavior: explicit empty tool lists, schema-aligned packaging instructions, original source references, withholding unresolved candidates, and the real attachment withdrawal lifecycle. Withdrawal now uses the production store and reconciliation function instead of reporting a missing adapter. No fake source deletion or oracle field injection is permitted. Four holdout cases stay unused in this development comparison.

The authorized cumulative reservation ceiling stays USD 20. A new trial records the prior terminal report by hash and carries every reservation and unresolved-call record, including the initial pilot. It never changes or resumes prior unknown calls. Ordinary resume continues to reject any unknown in its current trial. Known usage is an estimate, not an invoice. Semantic reviews remain unblinded, uncalibrated Codex case-author judgments and must be disclosed.
