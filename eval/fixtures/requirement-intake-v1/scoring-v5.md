# Requirement intake trial v5 — 2026-09-25

Same 12 development cases, original input/oracle/manifest, model alias, token and iteration limits, 120-second intake/review deadlines, and cumulative USD 20 ceiling as v4. Preserve baseline, trial-a (v3) and trial-b (v4), including their failures. No holdout case is used.

Production changes: the packaging brief factory supplies specific questions for missing required fields and restores them after narrative revision. Fulfilled fields lose only the exact Host-generated question; model-authored constraints remain. Explicitly undecided optional specifications remain narrative limitations. A rejected/withdrawn historical candidate is described as unusable, never pending reconfirmation of its old source. Units belong to measured values, not category/date/status labels. Proposed changes are emitted as unverified structured candidates; the Host retains the old verified value and blocks handoff until explicit confirmation, so the new proposal is observable without rewriting authority. No facts, permissions or approval are inferred by these changes.

Scoring correction: an exact finite numeric text representation versus a number (for example "0.06" versus 0.06) becomes needs_review, not automatically passed. A case-author reviewer must verify meaning against original evidence; unit, source, confirmation and state checks remain separate and cannot be bypassed. Different numeric values and strings with appended units remain deterministic failures when compared with a number. Historical reports are immutable. Publish separate rescore deltas without claiming any extra generation, checkpoint or completed task. Retain existing semantic decisions bound to exact capture hashes.

This is a combined repair on the development set, not an independent or single-factor causal test. Codex semantic reviews are unblinded and uncalibrated. Latency and cost cover different reached work; do not infer efficiency gains from early failures. Keep prior unknown calls/reservations and exclude their case scopes from any reviewed continuation. No production, multi-model, OCR, browser, real-model crash recovery or compaction claim is established here.
